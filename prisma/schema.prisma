generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model datasets {
  id          String @id @default(auto()) @map("_id") @db.ObjectId
  cid         String
  description String
  title       String
}
